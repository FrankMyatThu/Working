tbl_User
{
	ID,
	UserName,
	Password,
	SaltKey (To hash user's password)
}


tbl_ActiveUser
{
	ActiveUserID,
	UserId,
	IP,
	UserAgentID,
	JwtHMACKey (To set key for HMAC which will build over hashed jwt token)
}


****???? programmer can generate jwt token since he know JwtHMACKey.